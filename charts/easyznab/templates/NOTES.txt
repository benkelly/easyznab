1. Easyznab is exposed on port {{ .Values.service.port }} inside the cluster.
2. Run `kubectl get pods -l app.kubernetes.io/instance={{ .Release.Name }}` to monitor rollout.
3. If you enabled ingress, browse to one of the configured hosts to reach `/api`.
