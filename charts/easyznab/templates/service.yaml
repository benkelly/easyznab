apiVersion: v1
kind: Service
metadata:
  name: {{ include "easyznab.fullname" . }}
  labels:
    {{- include "easyznab.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "easyznab.selectorLabels" . | nindent 4 }}
